# Repository maintenance

- `ask-claude-for-codex/` is the independently installable package.
- `tests/` contains adapter regression tests. The package's `scripts/` is
  runtime code and ships with the Skill.
- Root README, changelog and license describe the product and its history.
  Development tests and CI configuration are not installed with the package.
